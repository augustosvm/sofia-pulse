sonar.projectKey=tiespecialistas_sofia-pulse
sonar.organization=tiespecialistas

sonar.projectName=Sofia Pulse - Tech Intelligence Platform
sonar.projectVersion=5.0

# Sources
sonar.sources=scripts

# Exclusions
sonar.exclusions=**/venv/**,**/node_modules/**,**/__pycache__/**,**/test_*.py,scripts/automation/**,analytics/**,db/**

# Python config
sonar.python.version=3.10
sonar.sourceEncoding=UTF-8

# 🎯 IGNORA PADRÕES INTENCIONAIS
sonar.issue.ignore.multicriteria=cc1,dup1,sql1

# Cognitive Complexity - Analytics SÃO complexos (parsing, ML, correlações)
sonar.issue.ignore.multicriteria.cc1.ruleKey=python:S3776
sonar.issue.ignore.multicriteria.cc1.resourceKey=analytics/**/*.py

# Duplicated literals - SQL/Queries têm strings repetidas (proposital)
sonar.issue.ignore.multicriteria.dup1.ruleKey=python:S1192
sonar.issue.ignore.multicriteria.dup1.resourceKey=**/*.py

# Illegal characters in SQL - Quebras de linha são normais em migrations
sonar.issue.ignore.multicriteria.sql1.ruleKey=plsql:S1192
sonar.issue.ignore.multicriteria.sql1.resourceKey=**/*.sql

# Quality gate
sonar.qualitygate.wait=false
